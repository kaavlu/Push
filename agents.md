# Push — Project Guide

## What is Push

Push is a private live map for real friends. The core value prop: **know the move before the group chat does.**

It helps close friends understand what's happening around them — where people are, what they're doing, who they're with, and whether something social is forming — without needing to text everyone.

Push is **not** a tracking app, not a generic map app, and not a chat app. It should feel like a premium Apple-native social layer for real life.

---

## Stack

- **Platform:** iOS
- **Framework:** SwiftUI
- **Target:** iOS 17+
- **Architecture:** MVVM
- **Data:** Parallel mock/live via `AppDataContainer` (mock `init(seed:)`; live `prepareLive`/`installPreparedLive`). `RootView` owns session bootstrap — mock skips auth; live restores session or `AuthGateView`, warms a session-scoped `LiveDataStore` (profiles/groups/memberships/policies/pushes/responses/presence/friendships/blocks), then installs `.shared` before `ContentView` ViewModels init. Sync `.live(...)` remains for isolated no-network tests. Live has no `InMemoryDatabase` (`database` nil). Live client: `supabase-swift` SPM (`Supabase` product, ≥2.0) — `import Supabase` only in auth/repo layer, not Views. Client credentials: committed `Push/Config/Supabase.xcconfig` (project URL + anon key only — never service-role); partial `Push/Info.plist` merges those keys with `GENERATE_INFOPLIST_FILE=YES` (Xcode does not inject arbitrary custom `INFOPLIST_KEY_*`). Read via `SupabaseConfig` (`.url` `fatalError`s unless host ends in `.supabase.co`; logs host via `PushLog.bootstrap`, never the anon key); use `SupabaseClientProvider.shared` in `Push/Data/Supabase/`. DEBUG defaults mock; `--live` opts in; Release always live. Day-1 live social graph reads (friends, groups, sharing policies) plus profile **self-writes** (including photo via Storage `avatars`, `0012`) and **push coordination** (`SupabasePushRepository` — create/edit/cancel/delete, RSVP; tables `pushes`/`push_responses`, migrations 0006–0008) — feed stays empty in live; presence reads/writes `current_presence` via `LiveDataStore`/`SupabasePresenceSync` + live friend patches via `PresenceRealtimeBridge` (`0020`) → `PresenceStatus` + synthetic `Place` (no mock data leaks; no places catalog); friend-request coordination via SupabaseAlertRepository + `FriendRepository` (search/send/cancel/remove; `0009`/`0010`/`0013`); group creation via `GroupRepository.createGroup` + `0011`; group invite accept/deny via `AlertRepository` + `0011`; group lifecycle RPCs + Storage `group-photos` in `0015` (rename/photo/invite/cancel/remove/leave/transfer/delete — all `SECURITY DEFINER`, no broad client writes on `groups`/`group_memberships`; mock lifecycle in `InMemoryDatabase+GroupLifecycle`/`LocalGroupRepository` mirroring `0015` rules — mock invite skips unknown IDs and does not require `acceptedFriendIDs`; live group lifecycle + photos via `SupabaseGroupRepository` → `LiveDataStore` → `0015` RPCs + `GroupPhotoStoring` (`group-photos` Storage; orphan rollback); user blocks (mock + live via `FriendRepository` block/unblock/`blockedUsers` + `0016` RPCs; soft-hide; `private.is_blocked` guards). Layout: `Push/Data/` — Domain, Seed, Store, Repositories, Derived, `AppDataContainer`. Spec: `tasks/spec.md` (Issue #27). Guides: `docs/data-architecture.md`, `docs/superpowers/specs/2026-07-05-data-architecture-design.md`, `docs/superpowers/plans/2026-07-12-supabase-migration-day1.md`. Supabase (`supabase/README.md`): `migrations/` (apply via MCP), idempotent `seed.sql` for public graph (test users via real Auth — never SQL into `auth.users`; `@pushapp.dev` emails, Confirm email OFF); use `.claude/skills/supabase*` skills. `group_memberships` is membership-only (no visibility column; `sharing_policies` is the sole visibility source per spec R3). `SECURITY DEFINER` RLS helpers (e.g. `private.is_friend`, `private.is_group_member`, `private.is_group_owner`, `private.is_blocked`, `private.shares_group`) live in non-API-exposed `private` schema — not `public` — so policies can call them without PostgREST RPC exposure.
- **Maps:** MapKit — live map base layer is satellite imagery (`MKImageryMapConfiguration`, flat elevation), not muted standard

This is a **high-fidelity prototype** that can become production later.

---

## MVP Features

1. **Live Map** — center of the app; friends shown with immediate social context
2. **Friend Status** — live status per friend (place, activity, availability, who they're with)
3. **Friend Detail** — tap a friend to see more; lightweight, not a full profile
4. **Feed** — real-life social activity (arrivals, availability shifts, groups forming)
5. **Who's Down** — quick answer to "is anything happening right now?"
6. **Pull Up** — low-pressure signal of social intent (faster than starting a group chat); creation UX is the 4-step **Start Push** flow (`StartPushFlowView`); launch from map create menu (`MainMapRoute.startPush`) or Pushes tab (`PlansView`).
7. **Friend Groups** — real-world circles with member statuses, activity, pushes
8. **Push Cards** — shared coordination objects (not chat threads)
9. **Privacy Controls** — simple visibility settings per activity

### Availability States
`Free now` · `Free soon` · `Maybe down` · `Busy` · `Joinable` · `Driving / ETA`

---

## What NOT to Build Yet

- Live writes to social graph (friends/groups/sharing), Realtime/subscriptions beyond `current_presence` — profile self-writes (basics, toggles, availability, photo), push coordination (create/edit/cancel/delete, RSVP), friend-request coordination (search/send/cancel/accept/deny via `0009`/`0013`; remove via `0010`/`0013`), group creation + group-invite coordination (`create_group` / `resolve_group_invite` via `0011`) + group lifecycle (`0015` RPCs + `group-photos` Storage), user block/unblock (`0016`), and presence Realtime (`PresenceRealtimeBridge`, migration `0020`) are allowed
- Push notifications
- iMessage extension
- Large groups
- Dating / social graph features

---

## Design Direction

**Feel:** Premium, Apple-native, social, lightweight, clear, calm, trustworthy, high-fidelity.

**Avoid:** Generic map app feel, surveillance dashboard feel, chat app feel, social media clone feel, enterprise dashboard feel.

**Glass + accents:** Brand colors live in `Push/PushColorPalette.swift` — walnut for foreground/text, sunbeam for active fills. Use `PushControlColors` text hierarchy (`textEspresso`, `textPrimary`, `textSecondary`, `textTertiary`); do not use black or system primary. Reuse `PushGlassStyle`, `PushControlColors`, and `PushControlStyle` for all glass controls; do not one-off material values. Prefer native `glassEffect` on iOS 26+ (layered with warm tint, stroke, shadow); iOS 17 uses the shared material fallback in `pushGlassBackground`. Hero map top controls use `ContentView`'s `topControlBackground` (material + warm tint), not `pushGlassBackground`.

**Adaptive layout:** `PushAdaptiveLayout` tiers by container width (compact <380, standard <420, large ≥420). `PushAdaptiveLayoutReader` at app root injects `@Environment(\.pushLayout)`; `*Style` helpers take `PushAdaptiveLayout` and use shared metrics (`pageHorizontalPadding`, `cardCornerRadius`, `puckScale`, etc.) — not one-off spacing constants. DEBUG previews: `PushPreviewMatrix` — wrap `*_Previews` in `#if DEBUG` so Release builds compile.

**Design reference (`Design/`):** Handoff bundle for visual work — `PushDesignBrief.md`, `PushThemeAudit.md` (historical pre-system snapshot; **superseded for implementation** by `docs/design-system.md`), verbatim snapshots in `CoreDesignFiles/`, copied imagery in `Assets/`. Read-only references; implement changes in `Push/` / `Push/DesignSystem/`, not in `Design/`.

**Design system (Issue #63, operational):** Waves 0–9 complete — catalog-driven for UI chrome; new families need a DS decision. Before adding UI chrome, open **`docs/design-system.md`** (catalog) and prefer `Push/DesignSystem/` components. Decisions: `tasks/design-system-decision-log.md` (DS-001–DS-090). Spec/waves: `docs/superpowers/specs/2026-07-21-push-design-system-specification.md`. Handoff: `tasks/design-system-handoff.md`. **Named surfaces only** — no local glass/cream recipes. Control → `pushControlGlass`; map chrome → `pushMapControlGlass` / `MapPopupSheetBackground`; pucks → `pushPuckGlass`; Plans/Review cards → `pushPlansCardGlass` / `pushReviewDeckGlass`; ivory → `PushIvoryPageBackground`; dense lists → `pushSolidCreamCard`; modal flows → `PushModalBackground`. **Primaries:** `PushSolidSunbeamButton` or `PushGlassRimButton` only (no `.borderedProminent` product chrome, no third primary). Generic circular utility → `PushCircleIconButton`. Person lists → flat `PushPersonRow` (blocked is config, not a fork); expand via `PushExpandablePersonRow` + rail. Group/history/section header are separate cream components. Cream selectors → `PushIvorySegmentedControl` / `PushIvoryFilterChipRow` (walnut selected) / `PushSingleSelectRow` / `PushModalChoicePill`; page header → `PushCreamPageHeader`; modal close → `PushModalCloseButtonBar`; map bottom sheets → `PushMapBottomSheetChrome` (custom chrome, not system `.sheet`); cream text links → `Text.pushTextLinkStyle()`. Full-page empty/loading/failed → `EmptySurfaceView` / `EmptySurfaceStateView` only; map emptiness → `MapEmptyOverlay`; mutations → `ActionErrorBanner` (not full-screen failed); destructive confirms → `.pushConfirmation` / `PushConfirmationDialog` (DS-090; window-level) — not system `confirmationDialog`; multi-action overflow menus → `.pushActionMenu` / `PushActionMenuDialog` (DS-090 companion; window-level) — not system `Menu`; multi-action photo menus keep system `confirmationDialog` (DS-066); info `.alert`s stay system. Availability colors → `PushAvailabilityTokens`; list/sheet availability → `PushAvailabilityChip`; person faces → `PushPersonAvatar` (dark/sunbeam); map pucks only via named puck family (no DIY). Plan cards (Wave 7) → `PushPlansPlanCard` (`.owner`/`.invited`; shims `YourPushCard`/`ActivePlanCard`), separate `PushReviewPlanCard` (shim `ReviewPushCard`); shared `PushPlanStatusPill`/`PushPlanAvatarStrip` — **leave `PlansCalendarView` whole**. Motion/opacity/radius/type (Wave 8) → `PushMotion` / `PushOpacityTokens` / `PushRadiusTokens` / `PushTypographyTokens` — pick named tokens; no new spring/duration/opacity/radius literals without promoting a token or DS decision. Onboarding/auth CTAs stay domain-local until a future alignment pass. Preserve approved appearance; extract/rename, do not redesign.

**Appearance:** Light-only for now — `UIUserInterfaceStyle` Light in `Info.plist` plus `PushApp` `.preferredColorScheme(.light)`. Brand hex and cream/glass chrome do not adapt; do not add Dark Mode variants or `@Environment(\.colorScheme)` branching unless explicitly scoped.

---

## Coding Standards

See `coding-standards.md` for the full reference. Key rules for this project:

- **MVVM strictly.** ViewModels own state and logic; Views are dumb.
- **Mock by default.** DEBUG uses mock unless `--live`; Release uses Supabase. No direct Supabase/network from Views or application ViewModels — injected repos only; auth via `AuthService` (`SupabaseAuthService` in `AuthService.swift`, `FakeAuthService` in `FakeAuthService.swift`; app layer uses `AuthViewModel` only). With supabase-swift, session user maps as unqualified `User`, not `Auth.User`. Mock: `NullLocationProvider` (no GPS); live: foreground when-in-use Core Location.
- **Profile images:** Seed/mock use bundle paths under `assets/friends|groups|profile|onboarding/`; mock group photo uploads use `GroupPhotoFileStore` (Application Support `group-photos/`); `deleteGroup` and sole-owner `leaveGroup` remove mock files. Live profile photos upload to Supabase Storage `avatars` (`0012`) with the public URL on `profiles.image_asset_path`; live group photos upload via `GroupPhotoStoring` (`GroupPhotoStorage.swift`) to Storage `group-photos` (`0015`) — owner-only writes under `{group_id}/…`, public URL on `groups.image_asset_path` (new object key per upload, like avatars). Resolve via `AvatarImageLoader` (bundle, local file, or HTTPS) — not `PushImageAssets` alone; avatar views use initials fallback when nil.
- **Seed data:** Single canonical source in `Push/Data/Seed/SeedData` (scattered `*MockData` / `RealWorldMockData` deleted — do not recreate for app screens). Opaque `String` IDs (seed may use readable slugs; never couple identity to display names). Group membership via `GroupMembership` rows, not stored `memberIDs`. Stats, social proof, timing labels, calendar rows, and map pucks are **derived** — never stored in seed. PuckLab and Onboarding Lab keep isolated design fixtures (`PuckLabFixtures`, `OnboardingLabFixtures`), not app data.
- **Repositories:** All protocols are `async throws` (local impls never throw); ViewModels take repos via init with optional `container: AppDataContainer? = nil` resolved as `?? .shared` in the init body (not `= .shared` in the default — mutable MainActor static). Expose primary content via `LoadState<Value>`. User-initiated edits persist via repo write methods (store + `didMutate()`), not ViewModel-local state only. Derived builders produce presentation structs from canonical domain data — presence builders use **`VisiblePresence`** (sharing-policy–filtered), never raw `PresenceStatus`; friend activity/venue labels via **`PresenceActivityPresentation`** only (not place-name recomposition in Views). Views must not read mock enums or seed directly.
- **Supabase UUID strings:** PostgREST returns lowercase UUIDs; `SupabaseAuthService` lowercases `AuthedUser.id` into `AppDataContainer.currentUserID` — do not compare DB-sourced id strings against raw `UUID.uuidString` (uppercase).
- **Diagnostics/logging:** `Push/Diagnostics/` — `PushLog.swift` (`os.Logger` categories: `bootstrap`, `network`, `auth`) for live startup and Supabase backend failures; wrap async backend calls with `PushLog.logged(_:)` — logs on failure, rethrows unchanged. **Crash/hang diagnostics:** Apple-native MetricKit via `CrashReporter` (`MXMetricManagerSubscriber`), started from `PushApp.init()` — logs crash/hang counts via `PushLog.bootstrap` on next launch; no third-party SDK. **PII redaction:** never log an error's `.localizedDescription` or a `PostgrestError`'s `.message`/`.detail`/`.hint` (constraint violations can embed user input); use `PushLog.safeDescription(for:)` — error type + Postgres `.code` only (e.g. `23505`).
- **Supabase row mapping:** PostgREST DTOs in `Push/Data/Supabase/Rows/` use snake_case properties matching table columns (no `CodingKeys`); map to domain via `*Row.*()` helpers. `timestamptz` columns decode as `String` and parse via ISO8601 (fractional seconds first) — not `Date`; default `JSONDecoder` date strategy fails on PostgREST strings. DB snake_case enums that don't match Swift `rawValue:` need explicit mapping — e.g. `profiles.availability_choice` (`free_now` → `FriendAvailabilityState`), `sharing_policies.audience_type` (`global_default` → `.globalDefault`). Profile toggle/connectors copy comes from `ProfileScaffolding`; live persists per-id enabled overrides in `settings_*` `jsonb` (`0005_profile_settings`), merged in `ProfileRow`. `GroupMembershipRow` sets `sharingLevel: .full` (R3). Live reads map from `LiveDataStore` (not per-call PostgREST); `SupabaseLiveDataLoader` owns network I/O. Initial warm fetches nine resources concurrently (profiles/groups/memberships/policies/pushes/responses/`current_presence`/friendships/blocked) with in-flight coalescing; one profiles response feeds current user, friends, and profile — current-user/`UserProfile` via `LiveDataStore.profile(userID:)` (case-insensitive match on `currentUserID`), never `profiles().first` (response order is undefined). `prepareLive` verifies that row exists before installing the container. `SupabaseFriendRepository` excludes self (RLS scopes friends/co-members; do not query `friendships`); `SupabaseGroupRepository` reads groups/memberships; `SupabaseSharingRepository` reads policies. Day-1 feed: `EmptyLiveFeedRepository` — empty reads. Alerts: live `SupabaseAlertRepository` (pending friendships + group invites via `0011` RPCs); `EmptyLiveAlertRepository` remains for inert tests. Pushes: `SupabasePushRepository` via `LiveDataStore` (session-cached like the social graph; `notifyPushesChanged()` clears cache and bumps once per logical write); `PushRow`/`PushResponseRow` + `PushDateFormatting`; recipient tokens via shared `PushRecipientResolver`. Other live repo writes throw `SupabaseRepositoryError.writeNotSupported` except profile: `SupabaseProfileRepository.updateBasics`/`updatePrivacy`/`updateProfilePhoto`/`removeProfilePhoto` (Storage via `ProfilePhotoStoring`; rollback orphan uploads on profile-write failure), `SupabaseFriendRepository.setCurrentUserAvailability` (social availability only via `set_availability_choice` dual-write — never `.ghost`; Ghost is `LocationSession.setPresencePublishingEnabled`), `SupabaseFriendRepository.sendFriendRequest`/`cancelFriendRequest`/`removeFriend`/`blockUser`/`unblockUser`/`blockedUsers`, `SupabaseGroupRepository.createGroup` + lifecycle mutations (rename/photo/invite/cancel/remove/leave/transfer/delete via `0015`; photo Storage via `GroupPhotoStoring` with orphan rollback; `deleteGroup` and sole-owner `leaveGroup` best-effort delete Storage objects), and `SupabaseAlertRepository` group-invite accept/deny.
- **Store refresh:** Mock: `InMemoryDatabase` bumps `@Published revision` via `didMutate()` after every write (never on reads). Live: `LiveDataStore` bumps revision on successful profile/availability write-through (PostgREST returned row replaces cache; failed writes leave cache/revision unchanged), on successful own-presence write-through (`upsertOwnPresence`/`unpublishOwnPresence`; failed sync leaves cache/revision unchanged), on `notifyPushesChanged()` after push writes, on `notifyFriendshipsChanged()` after friend-request writes (clears friendship/block session caches; accept/remove/block/unblock also drop profile warm cache), and on `notifyGroupsChanged()` after group create, invite accept, or any `0015` lifecycle write (clears group/membership cache). Full snapshot re-warm: `AppDataContainer.refreshSession()` → `LiveDataStore.refresh()` (mock no-op) clears all session caches and re-runs `warm()`; coalesces in-flight callers; debounces within `SessionRefreshConstants.minimumInterval`; failed re-warm restores prior snapshot without revision bump. Foreground: `ContentView` calls `refreshSession()` on return from background (skip first `.active` after bootstrap; failures silent). Pull-to-refresh: `FriendsViewModel`, `AlertsViewModel`, and `PlansViewModel` `refresh()` call `refreshSession()` then `load()`. Unprepared sync `.live(...)` falls back to a no-op subject for isolated tests. ViewModels subscribe via `AppDataContainer.onStoreChange`, guard on `lastSeenRevision` (stamp at end of `load()` to avoid reload loops), and call `load()` on change; reloads preserve last loaded value while refreshing. New mock mutating store methods must call `didMutate()`. Multi-row writes that must appear together (e.g. `createPush`, `updatePush` plan + responses) use a dedicated atomic store method with one `didMutate()`.
- **Recoverable mutation errors:** Failed user-initiated writes use `ActionErrorState` + shared `ActionErrorBanner` (inline message, Retry, dismiss; cream `friendsCard` styling — not system alerts). ViewModels expose `@Published actionError: ActionErrorState?` and keep retry payloads private; roll back optimistic UI on failure. Covered: Friends remove/block, Add Friends send/cancel/accept/deny, Alerts accept/deny, Profile unblock/availability/Ghost, Pushes RSVP/cancel/delete, Add Group create, Groups lifecycle (rename/photo/invite/cancel/remove/leave/transfer/delete via `GroupsViewModel`), Profile delete account.
- **Empty surfaces:** ViewModels derive `SurfaceContentPhase` from `LoadState` + emptiness; views render via `EmptySurfaceView` / `EmptySurfaceStateView` with copy from `EmptySurfaceCopy` and layout from `EmptySurfaceLayout` — no one-off empty strings or spacing. Map: `MapViewModel.surfacePhase` is `.empty` only when `friendsCount == 0` (Add friends CTA); friends who exist but aren't sharing leave `.content` with no overlay — `hasFriendMapContent` still drives puck rendering only. Initial map focus prefers live GPS via `LocationSession`, upgrading once over self-presence place; SF `MapDefaults` is fallback only. `ContentView` shows `MapEmptyOverlay` for `.empty`/`.failed` (Add friends → `.addFriend`, retry → `load()`); overlay spacers pass hits through so the map stays pannable. Map overlay is separate from cream-page empty views. Start Push step 1: `StartPushViewModel.surfacePhase` — `.empty` only when load succeeded with no friends and no groups; Add friends cover reloads recipients on dismiss; search no-match stays `.content`.
- **Live map:** `MapViewModel` owns puck `LoadState`, group filters (`GroupFilterItem`), `filteredPucks`, `selfPuck`, and `renderPucks(for:)`; `ContentView` tracks `mapSpan`, owns filter-dropdown expansion, bottom-nav tab overlays (Friends/Feed/Pushes embedded in its ZStack — not primary `fullScreenCover`s), and is render-only; tab overlays hide map chrome (top controls, puck sheets, empty overlay) and keep the floating bottom nav. Center + is contextual: Map → create menu; Friends → Add Friends; Pushes → Start Push; Feed → create-post hub (`CreatePostFlowView`, fixture UI). Two-stage derivation: `MapContentBuilder` → exact-place `MapPuckData`; `MapDisplayPuckBuilder` → zoom-aware `MapPuckRenderModel` (`.friend`, `.smallGroup`, `.selfPuck`, `.regionalCluster`). Close zoom (`latitudeDelta` ≤ 0.22) shows exact pucks; wider zoom clusters into `RegionalActivityPuck`. Vague-presence people use `Place.vagueCoordinate` for regional sources only. Regional cluster tap selects and shows `RegionalPuckDetailCard` (not `FriendDetailBottomSheet`); zoom via card CTA → `MapViewModel.focus(on:)`; empty map tap dismisses (`ContentView` owns `selectedRegionalPuck`, `StyledMapView` passes `selectedRegionalPuckID`). Regional puck layout/metrics → `PushRegionalPuckTokens` (DS-052 in `PushPuckGlass.swift`). `MapViewModel.select(personID:)` resolves an exact-place puck, resets filter to All Friends, and sets `mapFocusRequest` for programmatic focus (Friends avatar tap). Exact/group puck tap presents `FriendDetailBottomSheet` (custom bottom overlay, not SwiftUI `.sheet`); `FriendDetailSheet` is content-only — chrome, height, drag dismiss, and background live in `FriendDetailBottomSheet`. `MapPuckKind` drives content layout; multi-person pucks use `.joinable` availability. Filter by `groupIDs`; `FriendPuckData.id` is `String`. `StyledMapView` diffs puck annotations by ID — remove/add only stale entries.
- **Feed tab:** Two feed concepts — do not conflate: **Moment** media cards (`FeedMediaCarouselData`, Create Post, Add Yours; fixture-only today) vs legacy **Activity `FeedEvent`** (`LocalFeedRepository` seed / `EmptyLiveFeedRepository`; not wired to current Feed UI). Feed › Pushes = **Moments** stream (UI label may stay “Pushes”); Feed › Now deferred; Activity `FeedEvent` out of scope for Moment backend MVP. **Moment** ≠ coordination **Push** (optional one-to-one link after historical Push). Permissions enforced server-side per product contract. Backend S1 (`0021`/`0022`): `moments`/`moment_members`/`moment_media` (soft-delete; `UNIQUE(push_id)` incl. soft-deleted); friends-of-tagged SELECT RLS via `private.can_view_moment` (block-aware; no Ghost/sharing_policies). S2 (`0023`): mutation RPCs only (`create_moment`, `append_moment_media`, metadata/tags/reorder/soft-delete); SECURITY DEFINER; permission matrix server-side; max 8 media; `last_activity_at` bumps on create/append only. S3 (`0024`/`0025`): public Storage `moment-media` (`pending/{auth.uid()}/…` primary publish path + `{moment_id}/…` append; 100 MiB video cap; jpeg/png/webp + mp4/quicktime); `create_moment`/`append_moment_media` validate path ownership/mime/unregistered and derive `public_url`/`poster_url` server-side (caller URLs ignored). Client: `MomentMediaStoring` (`SupabaseMomentMediaStorage` / mock `LocalMomentMediaStorage` + `MomentMediaFileStore`); `MomentMediaPublisher` upload→RPC→orphan rollback (publish all-or-nothing; append per-item). Public bucket: known URLs stay fetchable (no hard revoke). Verify `supabase/tests/0021_moments_verify.sql` + `0023_moments_rpcs_verify.sql` + `0024_moment_media_storage_verify.sql` + `0025_moment_media_validation_verify.sql`. Feed repo/UI still empty/fixture (`EmptyLiveFeedRepository`; no publish/Add Yours upload wiring yet). Specs: audit `docs/superpowers/specs/2026-07-28-feed-moment-backend-requirements-audit.md` (#114); product contract `docs/superpowers/specs/2026-07-28-feed-moment-product-contract.md` (#115); backend architecture `docs/superpowers/specs/2026-07-28-feed-moment-backend-architecture.md` (#116). Bottom nav embeds `FeedView`/`FeedViewModel` in `ContentView` (leave via tab bar). Pushes/Now segments (Pushes default); group filter chips pinned under segment (`FeedFilterFixtures` until live group data), cream Friends styling. Pushes tab renders `PushMediaCarousel` stacks (compact 0.86 aspect, 30pt corners, fill-crop paging; Stories-style progress on every multi-item slide — active segment fills left-to-right during autoplay dwell; location pill + overflow on every slide; bottom row on slide 0 — multi-page embeds via `FeedMediaPageStrip.firstPageChrome` (swipes with first media; strip loops last→first via trailing clone); single-item uses card overlay; top metadata uses compact location pill + map-profile `pushMapControlGlass(.profileButton)` overflow (not cream `PushCircleIconButton`; pill height/radius matches overflow); bottom interaction via `FeedMediaBottomInteraction`/`PushMediaCarouselBottom` — participant avatar stack + names/contributor lines, play/pause, sunbeam Add yours when `FeedMediaCarouselData.canAddYours` → `fullScreenCover` `AddYoursView` (`AddYoursViewModel` local PhotosPicker draft + simulated submit; no uploads/feed mutation yet); bottom scrim; auto-advance gated by `FeedMediaVisibility` — primarily on-screen, not peeking neighbors) from `FeedMediaCarouselFixtures`/`FeedMediaModels` — presentation on `FeedMediaCarouselData`/`FeedMediaParticipant`; cream `FeedMediaCardContentSection` under media (title/meta; participant-only + / … → Add Yours or edit-moment `CreatePostFlowView` compose); tap media toggles autoplay pause/resume; full Push-card chrome still TBD; Now tab empty surface. Still no `FeedRepository`/`LoadState` (`EmptyLiveFeedRepository` unchanged). Feed header is title-only (`PushCreamPageHeader`); filter settings remain no-op. Center + → `CreatePostFlowView` (Pushes-style `PushCreamPageHeader` + close; create-from-scratch; `PushIvorySegmentedControl` Existing Moments / Past Pushes; compact solid-cream chooser rows; compose reuses Add Yours media stage; fixture/local only — no publish/feed append).
- **Friends screen:** Bottom nav Friends tab embeds `FriendsView` in `ContentView` (no page close — leave via tab bar; `FriendsLayout.contentBottomClearance`); `MainMapRoute.groups` remains a fallback `fullScreenCover` only. Friends mode: `FriendsViewModel` + `FriendsContentBuilder` — one row per direct friend via `VisiblePresence`; no visible presence → "Hidden right now" row (never dropped). `FriendsViewModel.surfacePhase`: zero friends → `.empty` + Add friends CTA (`EmptySurfaceCopy`); hidden-presence rows → `.content` (not `.empty`); search no-match stays `.content` with inline empty state. Filter chips (`showsFilterChips`) hidden when `friendsCount == 0`. Loading/failed use `EmptySurfaceStateView` (retry → `load()`). Friends list rows use `ExpandableFriendRow` (`PushPersonRow` inside) on the cream surface; row tap expands inline for Directions / Start push; avatar tap → `ContentView.locateFriendOnMap` via `onLocateFriend` (dismisses Friends, focuses exact-place puck — no-op when hidden/vague); long-press opens Remove/Block overflow — not `FriendDetailBottomSheet` (map puck tap still uses that sheet). Remove via `FriendsViewModel.removeFriend` → `FriendRepository.removeFriend`. Group member rows still use `FriendRowCard` directly. Groups mode presents `GroupDetailHost` via `fullScreenCover` (Profile-style slide; same from standalone `GroupsView`); `GroupManageView` uses a nested cover from `GroupDetailView` — not inline list replacement. Host wires `GroupsViewModel` lifecycle mutations into dumb detail/manage views; shared chrome in `GroupDetailManageViews`/`GroupDetailSheets`. `GroupDetailHost` re-resolves the open group from the view model after rename/photo writes. Cream page styling (`FriendsColor`, `friendsCard`), not map glass. DEBUG launch: `--friends`.
- **Alerts:** Map hero bell (`MainMapRoute.alerts`, unread dot) presents full-screen `AlertsView`; friend requests (`FriendRowCard`) and group invites (`GroupRequestCard`) with shared accept/deny timing via `AlertsViewModel.resolve`. Pending group invites count toward the unread badge (no per-row read state). Soft reload: keep last loaded content visible while refreshing; accept/deny failures use `ActionErrorBanner`, not full-screen `.failed`. `AlertsViewModel` + `AlertRepository`; mock `LocalAlertRepository` resolves via store; live `SupabaseAlertRepository` (`0009` friend RPCs + `0011` group-invite RPCs). Replaces `MKCompassButton`; map attribution unchanged. Cream Friends-page styling (`AlertsStyle`).
- **Add Friends:** Full-screen `AddFriendsView` (cream Friends treatment) from map create menu (`MainMapRoute.addFriend`) or Friends tab center + (contextual — skips create menu). Search/send/cancel via `FriendRepository` (`searchPeople`, `sendFriendRequest`, `cancelFriendRequest`); accept/deny reuse `AlertRepository`. Mutation failures use `ActionErrorBanner` with optimistic rollback (not full-screen `.failed`). Mock: `acceptedFriendIDs` + discoverable seed people (austin/jordan). Live: `search_profiles` / `send_friend_request` / `cancel_friend_request` RPCs (`0009`/`0013`); store invalidates profile cache on friendship writes so Friends/Add Friends refresh.
- **Add Group:** 3-step full-screen `AddGroupFlowView` (name + optional photo, 2–19 friend invitees, review) from Friends Groups mode “+”. `AddGroupViewModel` → `createGroup` with nil `imageAssetPath`, then `updateGroupPhoto` for picked JPEGs; create failure is retryable; post-create photo failure keeps the review step with `retryPhotoUpload` or Continue without photo (`lastCreatedGroupID`). `GroupsViewModel.registerSessionImage` remains for instant detail display before reload. Cream Friends styling (no numbered step indicator). Live: `create_group` RPC (`0011`); invitees accept via Alerts.
- **Onboarding Lab:** DEBUG-only 12-screen signup primer in `Push/OnboardingLab/`; `OnboardingLabViewModel` + screen splits (`*IntroScreens`, `*AuthScreens`, `OnboardingSignInScreen`, etc.), static `OnboardingLabFixtures` only — no repos/auth/backend in the lab. Parallel returning-user entry: welcome ↔ `.signIn` (`OnboardingAuthSwitchLink`). The theme + components (`OnboardingLabTheme`, `OnboardingLabComponents`, `OnboardingAuthComponents`) are promoted out of `#if DEBUG` for production reuse; the lab screens, fixtures, keypad flow, setup, and mock `completeSignIn()` stay `#if DEBUG`. DEBUG launch: `--onboardinglab`; jump to one screen with `--screen=<rawValue>` (e.g. `--onboardinglab --screen=signIn`).
- **Production auth:** `PushApp` → `RootView`; live unauthenticated users see `AuthGateView` (lab shell: warm gradient + back chrome) routing `AuthViewModel.screen` across welcome, multi-step email sign-up (`signUpProfile` → `signUpCredentials`), signIn, checkEmail, forgotPassword, setNewPassword. UI reuses onboarding components + lab welcome hero fixtures (`AuthHeroFixtures`). Email auth is fully live via `AuthService`/`AuthViewModel` only (no Supabase in Views): sign-up with name/handle/optional photo (`SignUpResult` → session or check-email; photo JPEG held then uploaded after `prepareLive` via `ProfileRepository.updateProfilePhoto`, soft-fail); sign-in, forgot/set password; user errors via `AuthUserMessage`. Confirm-email redirect is `pushapp://auth/callback` (not reset). Reset links use `pushapp://auth/reset` — `RootView.onOpenURL` → `handleOpenURL`; recovery holds the gate until password update. Google (OAuth `ASWebAuthenticationSession` → `pushapp://auth/callback`) uses the same post-auth prepare path; cancellations are silent. Sign in with Apple is removed (no entitlement) so Personal Team provisioning works. OAuth profile rows use migration `0017` (`handle_new_user` handle/name hardening). Fresh sign-in/restored session enter `.preparing` until snapshot warm (Retry/Sign Out on failure). **Post-auth onboarding (new accounts, `0019`):** when `profiles.onboarding_completed_at` is null, `RootView` shows `PostAuthOnboardingView` — privacy → `set_global_sharing_defaults` + Ghost/`updatePrivacy`; location → `LocationSession.startIfEligible`; notifications → system `UNUserNotificationCenter` (no push backend yet); find friends → `discover_profiles` + `sendFriendRequest`; complete → `complete_onboarding` then app. Existing profiles were backfilled complete. Mock never shows this flow. Phone auth remains DEBUG lab only. Spec: `docs/superpowers/specs/2026-07-24-production-lab-auth-ui-design.md`.
- **Account deletion (Issue #48):** Design spec at `docs/superpowers/specs/2026-07-20-account-deletion-design.md`. Live-only Profile destructive control; `AuthService.deleteAccount()` → parameterless `delete_account` SECURITY DEFINER RPC (migration `0014`); `RootView` injects `DeleteAccountAction` and only enters the auth gate after RPC success (never sign out first); failures stay signed in with recoverable error; mock hides control; server handles group ownership transfer-then-delete.
- **Location/presence (Issue #64):** Spec `docs/superpowers/specs/2026-07-23-location-presence-architecture-design.md`. Map/presence **presentation** is complete (`VisiblePresence` → builders); live reads via `current_presence` + synthetic `Place` through existing repos/builders — no parallel map stack. **PR5b (#75):** live `SupabasePresenceSync` (`PresenceSyncing`) — latest-draft buffer + single-flight flush (failed writes keep newest draft only); PostgREST upsert + `unpublish_current_presence` RPC (unpublished drafts skip coord upsert); success write-through via `LiveDataStore.upsertOwnPresence`/`unpublishOwnPresence` (`CurrentPresenceWriteMapping`); live via `AppDataContainer.makeLiveLocationSession`, mock via `LocalPresenceSync`; `sync.shutdown()` on session teardown drops pending retries. **PR5c (#76):** `PresencePublishPolicy` movement gate (60s/50m + first fix) + 15m heartbeat; Ghost unpublish/republish on `LocationSession.setPresencePublishingEnabled`; profile availability via `set_availability_choice` dual-write (location mirrors profile only). **PR5a (#73):** `LiveDataStore.currentPresence()`/`SupabaseLiveDataLoader.loadPresence()` session cache; `CurrentPresenceRow` → `PresenceStatus` + synthetic `Place` (`presence-<user_id>`); `SupabaseFriendRepository.presenceStatuses()`/`SupabasePushRepository.allPlaces()` filter hard-expired/unpublished (non-self); `VisiblePresenceBuilder` guards `isEffectivelyPublished`; `notifyPresenceChanged()` for Realtime/external invalidation. **Domain foundations (#66):** app-owned `LocationObservation`/`PresenceStatusDraft` and injectable protocol seams (`LocationProviding`, `LocationSessioning`, `PresenceSyncing`, etc.) in `Push/Data/Domain/Location*.swift` + `PresenceDrafts.swift` — Doubles only, no `CLLocation` in domain; `PresenceStatus.isPublished`/`isEffectivelyPublished` + `PresenceFreshness` for friend visibility. **PR2 (#68):** `SimulatedLocationProvider` + `SimulatedLocationRoute`/`SimulatedLocationRouteFixtures`; pure `LocationObservationValidator` + `GeoDistance`; thresholds in `LocationPipelineConstants`. **PR3 (#69):** `LocationSession` on `AppDataContainer` (not `MapViewModel`) — provider → validator → inferrer → sync; `LocationSessionFactory` (mock → `NullLocationProvider`; live → `CoreLocationLocationProvider` when-in-use; DEBUG `--sim-location` overrides both → `SimulatedLocationProvider` in `.timed` mode with wall-clock `baseDate`; `--fast-presence-heartbeat` → 20s stationary heartbeat vs production 15m; tests use `.manual` / frozen `baseDate`). **PR7 (#79):** `CoreLocationLocationProvider`/`CoreLocationMapping` in `Push/Data/Location/` — `CoreLocationManaging` test seam; `NSLocationWhenInUseUsageDescription` in `Info.plist`. Lifecycle: `installPreparedLive` shuts down prior session without unpublish; sign-out/delete via `shutdownSharedAndReinstallMock` (best-effort unpublish → shutdown → mock reinstall; skip unpublish after account deletion). **PR4 (#71):** migration `0018_current_presence` — one row/user, orthogonal `is_published`, friend-visible RLS (block/publish/expiry/legacy-ghost gates), RPCs `unpublish_current_presence`/`set_availability_choice` (availability dual-write); no seed rows; verify `supabase/tests/0018_current_presence_verify.sql`. `import CoreLocation` / `CLLocation*` **infrastructure-only** behind `LocationProviding`. Ghost → orthogonal `is_published` (not availability); canonical availability `profiles.availability_choice`, dual-written to presence. **PR9 (#84):** `PresenceRealtimeBridge`/`PresenceRealtimeApplying` in `Push/Data/Supabase/` — postgres_changes on `current_presence` patches `LiveDataStore` via remote apply/remove/reconcile + debounced revision (ignore own rows; mock never starts; migration `0020_current_presence_realtime`); `AppDataContainer.installPreparedLive` → `start()`, `stopPresenceRealtimeBridge` on swap/sign-out. **I1 (#92):** activity inference domain in `Push/Data/Domain/ActivityInference*.swift` — `InferredActivityKind`/`InferredActivityResult`, injectable `ActivityInferenceEngine` (optional `previous` for hysteresis), `ActivityInferenceConfiguration`, `ActivityObservationWindow`, `ActivityInferenceFixtures`. **I2 (#93):** `DeterministicActivityInferenceEngine` — speed/displacement/dwell rules, accuracy filtering, confidence mapping, hysteresis; no venues/co-presence/availability. **I3 (#94):** `LocationSession+Pipeline`/`LocationSessionActivityState` run observation-window inference (default `DeterministicActivityInferenceEngine`); `ActivityInferencePresentation` maps kinds → existing `activity_name`/`activity_symbol` draft fields (no schema migration; Realtime/LiveDataStore unchanged); heartbeats/republish hold last valid activity; unknown inference never blocks publish (`.nearby` / last-valid fallback). **I1 (#99):** dwell detection domain in `Push/Data/Domain/DwellDetection*.swift` — `DwellPhase`/`DwellDetectionState`/`DwellClusterSnapshot`, injectable `DwellDetecting` (`DeterministicDwellDetector`); `LocationSession+Pipeline` updates `dwellState` in parallel (no draft/label/publish/availability changes). **I2 (#100):** arrival/departure lifecycle — `DwellTransition`/`DwellLifecycleSession`, one-shot `.arrived`/`.departed` with departure hysteresis; `activeSession`/`lastCompletedSession` on `DwellDetectionState` (venue-resolution seam); still no draft/label/publish/availability changes. **I3 (#101):** place resolution for confirmed dwells — `PlaceResolving`/`PlaceResolution*` domain + pure `PlaceCandidateRanker` in `Push/Data/Domain/PlaceResolution*.swift`; live `MapKitPlaceResolver` (MapKit POI search + reverse-geocode fallback) in `Push/Data/Location/`; mock/tests `NoOpPlaceResolver`/`FixedPlaceResolver`; `LocationSession+PlaceResolution` schedules lookups on arrival/centroid change/retry only (clears on departure); `activePlaceResolution` on session; factory wires live → `MapKitPlaceResolver`, mock → `NoOpPlaceResolver`; feeds presence activity in #105. **I4 (#105):** `ActivityInferencePresentation.compose` merges confident `activePlaceResolution` → `At {place}`, confirmed dwell without place → Chilling, else Walking/Driving/Moving/Nearby onto draft `activity`/`statusNote`/`placeID`; session republishes on place attach, arrival Chilling, and departure clear — existing `current_presence` fields + Realtime/`LiveDataStore` (no schema migration; availability/Ghost unchanged). **I5 (#109):** `PresenceActivityPresentation` (`Push/Data/Derived/`) — sole mapping from presence `activity`/`statusNote`/place → list/map/detail/group/profile labels and symbols; `FriendsContentBuilder`/`MapContentBuilder`/`GroupContentBuilder`/`ProfileContentBuilder`/`FriendDetailSheet` call it; Views/ViewModels must not re-compose activity from place names or synthetic `At {motion}` copy; availability stays independent of activity. Phase 1 non-goals: background location, places catalog, place-correction UI.
- **Legal:** `LegalDestinations` is the single source of truth for Terms/Privacy URLs and row metadata; `LegalConsentText` renders tappable inline consent on auth welcome and onboarding lab; Profile Legal card opens the same destinations externally. Placeholder URLs in `LegalDestinations.swift` are a **release blocker** — replace with public HTTPS documents before App Store distribution. App Store Connect disclosure inputs: `docs/app-store-privacy.md` (re-audit before submission).
- **Friend Groups:** `GroupContentBuilder` (`Push/Data/Derived/`) derives `PushGroupData` cards and `PushGroupMemberData` rows (availability, venue/status text, activity symbol, relative last-updated, `membershipID`, `isOwner`, `isPending`) from memberships + `PresenceStatus` + `Place` — never stored in seed; canonical presence wins over legacy group-table values; `PushGroupMemberData.id` is person id (`membershipID` for lifecycle mutations). `GroupsViewModel` (`GroupsViewModel.swift`) owns list/detail state, lifecycle mutations via `GroupRepository`, and membership helpers (`currentUserMembership`/`isCurrentUserOwner`/`inviteCandidates`); optimistic rename rollback and session image overrides for in-flight photo updates. Group detail: rename/photo/members/Start push on `GroupDetailView`; invite/transfer/leave/delete on `GroupManageView` (Manage button). Member remove/cancel stay on detail. Host via `GroupDetailHost` only — do not wire mutations directly in list views. Group cards in `FriendsView` Groups mode use the same derived data.
- **Push lifecycle:** `PushLifecycle` (`Push/Data/Derived/`) derives active/historical/happening at read time from `startsAt`/`expiresAt` — cancelled excluded; do not rely on persisted `PushPlan.state` for surfacing. `PushRepository.activePlans()` / `historicalPlans(forMonthContaining:)` filter via `PushLifecycle`; calendar rows via `PastHangoutBuilder`, History list via `HistoryContentBuilder` (mock merges seed hangouts for calendar richness; live derives from completed pushes only).
- **Pushes tab cards:** `PlansContentBuilder` (`Push/Data/Derived/`) derives `PlanData`, hangout calendar, and most-active-group from active `PushPlan`/`PushResponse`/`PastHangout`; timing via `PushTimingFormatter`. Card status pills reflect the viewer's `PushResponse`, not `PushPlan.state`. Also derives `participants`, `maybeParticipants`, creator `note`, place `address`, and `distanceLabel` (great-circle miles from self presence place — not real GPS). `PushPlan.groupID` and `placeID` are optional — nil for invitees-only pushes (no group) or Start Push free-text location (`locationText` → `locationHint` when `placeID` is nil).
- **Profile:** `ProfileContentBuilder` derives `ProfileData` from `UserProfile`, `Person`, and self-scoped `VisiblePresence`; toggle/connectors copy from `ProfileScaffolding` (mock seed and live `ProfileRow` share this source). `ProfileViewModel` write-through: availability → `FriendRepository.setCurrentUserAvailability` (social states only; live dual-writes via `set_availability_choice`); Ghost → `LocationSession.setPresencePublishingEnabled` (orthogonal unpublish — supports Busy+Ghost; never writes `.ghost` availability); basics → `ProfileRepository.updateBasics`; privacy toggles → `updatePrivacy` (id→enabled maps only — copy stays client-side); photo → `ProfileRepository.updateProfilePhoto`/`removeProfilePhoto` after `ProfilePhotoProcessor` resize/compress (PhotosPicker in view). Place line uses vague label (`Near …`), not exact venue. Blocked list via `BlockedUsersView`/`BlockedUsersViewModel` (`fullScreenCover` from Profile); unblock does not restore friendship.
- **Current user on map:** Inside exact/group pucks when `isCurrentUser: true`; otherwise `SelfPuckView` at close zoom. No standalone `UserLocationPin` on the live map.
- **Map attribution:** Set `MKMapView.layoutMargins` via `StyledMapView.mapLayoutMargins` (`MapAttributionLayout` in `ContentView`) for Apple logo and legal text only (`showsCompass = false`; no manual compass — hero bell is in `ContentView`). Keep bottom insets low so attribution sits discreetly at the screen edge, not above the floating nav. Update insets when top/bottom UI changes.
- **Pushes tab:** Cream Friends-page treatment (`FriendsBackground`, `FriendsLayout` header spacing), not `PushModalBackground`. Embedded under `ContentView` bottom nav (no page close; `PlansLayout.contentBottomClearance`). `PlansView` splits owned vs invited pushes — `PlanData.isOwner`; `PlansViewModel.yourPushes` / `activePushes`. Weekly recap card (`PlansCalendarView` + `PlansWeeklyRecapDayTile`) shows a Monday-first week with `moveWeek` navigation (reloads when month changes); day taps open detail only when `pushCount > 0` or `almostHappened`. History › (`PlansViewModel.showsHistoryLink` / `hasWeekHangoutSummary`) opens `PlansHistoryView` (month list; rows display-only — no detail sheet on tap). Calendar and push cards use `plansGlassCard` / `PlansColor` with walnut borders matching Friends cards; Start Push via bottom-nav + on Pushes tab (no inline CTA). Owned preview uses `YourPushCard` ("Manage →" opens `StartPushFlowView(context: .edit(plan:))` via `managedPlan`); `YourPushesListView` uses the same edit cover locally; invited tab preview uses `ActivePlanCard`; full swipe deck in `ReviewPushesView` uses `ReviewPushCard` with `reviewGlassCard` / `ReviewGlassStyle`.
- **Start Push flow:** `StartPushFlowView(context:)` accepts optional `StartPushLaunchContext` to pre-fill recipient tokens, location hint, and starting step (friend/group/map puck → skip to details when recipients are known). Present via dedicated `fullScreenCover` after dismissing friend/group sheets (`ContentView`, `FriendsView`, `GroupsView`); blank context from map create menu (`MainMapRoute.startPush` / `.startPlan`) or Pushes tab bottom-nav +. Edit owned pushes via `.edit(plan:)` (immediate `PlanData` prefill) or `.edit(planID:)` (repo load); edit starts at recipients, submit title is "Save changes", and `goBack` from confirmation resets `hasSubmitted`. `StartPushViewModel` loads groups/friends from `AppDataContainer` repos with `LoadState`; Step 4 suggestion buckets (`likelyFreeNow`, `mightBeInterested`) derive from canonical `PresenceStatus` availability (`.freeNow`/`.joinable` vs `.maybeDown`/`.freeSoon`) — never hardcode recipient IDs. `submit()` runs on step 3→4 (`StartPushFlowView.submitAndAdvance`), before confirmation; idempotent via `hasSubmitted` claimed before `await` (not after repo write returns). Submit via `PushRepository.createPush(_:)` or `updatePush(planID:with:)` when `editPlanID` is set, using `PushDraft` (`recipientIDs` are flow tokens `"group_<id>"` / `"friend_<id>"`); single selected group → `audience: .group` + `groupID`, otherwise `.inviteesOnly`; creator gets `.in`, deduped invitees `.pending` (creator excluded). Edit recipient prefill prefers visible `.in` people over the stored group token.
- **Feature files:** Flat under `Push/` — split by suffix: `*Models`, `*View`, `*ViewModel`, `*Style`. App data flows through repos + derived builders, not `*MockData` files. Multi-step flows add `*FlowView` (container), `*StepNView`, and shared `*Style`; register on `MainMapRoute`.
- **Xcode registration:** Register every new Swift file in `Push.xcodeproj` via `python3 scripts/pbxproj_add.py <path>` (paths relative to `Push/`; `--target tests` for `PushTests/`). Idempotent — safe to re-run.
- **Repo hygiene:** Do not commit `DerivedData/`, `build/`, `*.xcresult`, `**/xcuserdata/`, local scratch logs, or secrets (`.env`, certs, `.supabase/`, credential filenames) — all covered by `.gitignore`.
- **Tests:** Prefer `scripts/test.sh` (separate `DerivedData-Tests/`). Destination defaults to the worktree visual sim via `run-ios-sim.sh ensure-booted-udid` (`Push - main - iPhone 17` on primary checkout — **not** stock unlabeled `iPhone 17`, which opens a second flaky Simulator). Override with `PUSH_TEST_DESTINATION`. Scoped by area — do **not** run the full suite after every file edit. Seed/domain: `scripts/test.sh suite DataLayerTests`. Live/mock isolation: `LiveContainerIsolationTests`. Auth bootstrap: `AuthBootstrapTests`. Production auth flows: `AuthViewModelTests`. Post-auth onboarding: `PostAuthOnboardingTests`. Account deletion: `DeleteAccountTests`. Live snapshot: `LiveDataStoreTests`. Live presence reads: `LivePresenceReadTests`. Live presence writes: `LivePresenceWriteTests`. Realtime presence bridge: `PresenceRealtimeTests`. Map pucks: `MapRenderTests` / `MapPuckHitTestingTests`. Layout: `AdaptiveLayoutTests`. Supabase mapping: `SupabaseMappingTests`. Live push repo: `SupabasePushRepositoryTests`. Push lifecycle: `PushLifecycleTests`. Alerts: `AlertsTests`. Add Friends: `AddFriendsTests`. Friend lifecycle: `FriendRelationshipTests`. Profile photos: `ProfilePhotoTests`. Group lifecycle: `GroupLifecycleTests`. Block user: `BlockUserTests`. Location/presence foundations: `LocationPresenceFoundationTests`. Activity inference domain: `ActivityInferenceTests`. Deterministic inference rules: `DeterministicActivityInferenceTests`. Activity inference pipeline: `ActivityInferenceIntegrationTests`. Dwell detection: `DwellDetectionTests`. Dwell lifecycle: `DwellLifecycleTests`. Place resolution: `PlaceResolutionTests`. Presence activity composition: `PresenceActivityCompositionTests`. Presence activity presentation: `PresenceActivityPresentationTests`. Location observation validation: `LocationObservationValidatorTests`. Simulated location provider: `LocationSimulatedProviderTests`. Core location provider: `CoreLocationProviderTests`. Location session: `LocationSessionTests`. Location throttle/heartbeat: `LocationPresenceThrottleTests`. Publish policy: `PresencePublishPolicyTests`. Container wiring: `LocationSessionContainerTests`. Legal: `LegalDestinationsTests`. Empty surfaces: `EmptySurfaceTests`. Feed shell: `FeedViewModelTests`. Feed media carousel: `FeedMediaCarouselTests`. Add Yours: `AddYoursViewModelTests`. Create post: `CreatePostViewModelTests`. Moment media Storage: `MomentMediaStorageTests`. Confirmation dialogs: `PushConfirmationTests`. Logging/redaction: `PushLogTests`. Supabase config guard: `SupabaseConfigTests`. Build-only: `scripts/test.sh build`. Before commit/PR or cross-cutting changes: `scripts/test.sh full` (serial; parallel testing intermittently drops the simulator runner). Do not run `PushUITests` unless asked. SourceKit may false-positive on `Supabase` imports — trust `xcodebuild` / `scripts/test.sh` (see `tasks/lessons.md`).
- **iOS Simulator:** `scripts/run-ios-sim.sh [run|stop|restart|status|list|prune|reload-if-booted|ensure-booted-udid] [--iphone-17|--iphone-17-pro-max|--all] [-- app args…]`. Primary checkout always uses label `main` → visual sim `Push - main - iPhone 17` (stable across feature branches). Orca worktrees under `…/orca/workspaces/Push/<name>` use that name. Default `run` reloads booted worktree sims or creates iPhone 17; stop-hook uses `reload-if-booted` (no create). `ensure-booted-udid` create/boots the preferred worktree sim and prints its UDID (used by `scripts/test.sh`). `list` / `prune` manage Push-prefixed devices. Pass DEBUG launch args after `--` (e.g. `scripts/run-ios-sim.sh -- --friends`).
- **Stop hook:** `.claude/settings.local.json` Stop → `scripts/stop-hook-run.sh`. Modes in `.claude/run-mode`: `live` (default), `mock`, `off`. Locks + 45s debounce; skips when no Swift/project changes; only reloads if `Push - main - iPhone 17` is already Booted.
- **Files ≤ 400 lines.** Split by responsibility.
- **Functions ≤ 40 lines, single responsibility.**
- **No magic numbers.** Named constants only.
- **Comments explain WHY, not WHAT.**
- **Spec before code** for non-trivial features. Write `tasks/spec.md` first.
- **Commit frequently.** At least after each logical component.

### Task Files

| File | Purpose |
|---|---|
| `README.md` | Human onboarding: product overview, mock vs live, setup, testing, screenshots |
| `tasks/todo.md` | Current plan and progress tracking |
| `tasks/spec.md` | Active feature spec (write before implementation) |
| `docs/data-architecture.md` | Seed workflow, derivation rules, test suites, Supabase migration seam |
| `docs/design-system.md` | Agent UI catalog — open before adding chrome (Issue #63) |
| `docs/app-store-privacy.md` | App Store Connect privacy disclosure inventory; re-audit before submission |
| `docs/superpowers/specs/*.md` | Dated design specs per feature; read the relevant file before implementing |
| `docs/superpowers/plans/*.md` | Step-by-step implementation plans for multi-task rollouts; follow task-by-task |
| `tasks/design-system-handoff.md` | Design-system historical wave tracking (Issue #63 complete) |
| `tasks/design-system-decision-log.md` | DS-001–DS-090 product decisions (law) |
| `tasks/lessons.md` | Project-specific learnings and gotchas |

### Session Resume Protocol

Read: `CLAUDE.md` → `tasks/lessons.md` → `tasks/todo.md` → `git log --oneline -5`. Supabase/backend work: also read `supabase/README.md`, `tasks/spec.md` (Issue #27), and repo Supabase skills. Feed/Moment work: also read `docs/superpowers/specs/2026-07-28-feed-moment-backend-requirements-audit.md` (#114), `docs/superpowers/specs/2026-07-28-feed-moment-product-contract.md` (#115), and backend architecture `docs/superpowers/specs/2026-07-28-feed-moment-backend-architecture.md` (#116); `AGENTS.md` Feed tab bullet. Location/presence work: also read `docs/superpowers/specs/2026-07-23-location-presence-architecture-design.md` (Issue #64). Realtime presence sync (Issue #84): read design `docs/superpowers/specs/2026-07-24-realtime-presence-sync-design.md`; implementation `docs/superpowers/plans/2026-07-24-realtime-presence-sync.md`. UI chrome: open `docs/design-system.md` first (Issue #63 operational; new families need a DS decision); `Design/PushThemeAudit.md` is read-only history superseded for implementation.

Do not ask the user to re-explain context that is in these files.

---

## Status Language

Status copy should feel **natural, casual, and socially safe.** When confidence is high, be specific. When confidence is lower, soften the wording. Never make it feel like surveillance.

User-facing coordination copy uses **Push/Pushes** (not Plan/Plans). Internal types and files may still use `Plan*`/`Plans*` prefixes until refactored.
